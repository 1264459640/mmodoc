# 核心代码类分析

## 1. ItemManager

### 1.1 客户端 ItemManager

**文件**: `Src/Client/Assets/Scripts/Managers/ItemManager.cs`

```csharp
class ItemManager : Singleton<ItemManager>
{
    public Dictionary<int, Item> Items = new Dictionary<int, Item>();

    internal void Init(List<NItemInfo> items)
    {
        this.Items.Clear();
        foreach (var info in items)
        {
            Item item = new Item(info);
            this.Items.Add(item.Id, item);
        }
        StatusService.Instance.RegisterStatusNofity(StatusType.Item, OnItemNotify);
    }

    private bool OnItemNotify(NStatus status)
    {
        if (status.Action == StatusAction.Add)
        {
            this.AddItem(status.Id, status.Value);
        }
        if (status.Action == StatusAction.Delete)
        {
            this.RemveItem(status.Id, status.Value);
        }
        return true;
    }
}
```

**职责**：
- 维护客户端全部道具资产
- 监听状态同步消息
- 把数量变化同步给 `BagManager`
- 作为装备、背包、任务奖励等系统共享的数据源

**关键理解**：
- 它关心“玩家拥有什么”，不关心“显示在哪个格子”。

### 1.2 服务端 ItemManager

**文件**: `Src/Server/GameServer/GameServer/Managers/ItemManager.cs`

```csharp
class ItemManager
{
    Character Owner;
    public Dictionary<int, Item> Items = new Dictionary<int, Item>();

    public bool AddItem(int itemId,int count)
    {
        Item item = null;
        if (this.Items.TryGetValue(itemId,out item))
        {
            item.Add(count);
        }
        else
        {
            TCharacterItem dbItem = new TCharacterItem();
            dbItem.CharacterID = Owner.Data.ID;
            dbItem.Owner = Owner.Data;
            dbItem.ItemID = itemId;
            dbItem.ItemCount = count;
            Owner.Data.Items.Add(dbItem);
            item = new Item(dbItem);
            this.Items.Add(itemId, item);
        }
        this.Owner.StatusManager.AddItemChange(itemId, count, StatusAction.Add);
        return true;
    }
}
```

**职责**：
- 维护角色真实道具资产
- 处理加减道具
- 通过 `StatusManager` 推送变化给客户端

---

## 2. BagManager

**文件**: `Src/Client/Assets/Scripts/Managers/BagManager.cs`

```csharp
class BagManager : Singleton<BagManager>
{
    public int Unlocked;
    public BagItem[] Items;
    NBagInfo Info;

    public void Reset()
    {
        int i = 0;
        foreach (var kv in ItemManager.Instance.Items)
        {
            if (kv.Value.Count <= kv.Value.Define.StackLimit)
            {
                this.Items[i].ItemId = (ushort)kv.Key;
                this.Items[i].Count = (ushort)kv.Value.Count;
            }
            else
            {
                int count = kv.Value.Count;
                while (count > kv.Value.Define.StackLimit)
                {
                    this.Items[i].ItemId = (ushort)kv.Key;
                    this.Items[i].Count = (ushort)kv.Value.Define.StackLimit;
                    i++;
                    count -= kv.Value.Define.StackLimit;
                }
                this.Items[i].ItemId = (ushort)kv.Key;
                this.Items[i].Count = (ushort)count;
            }
            i++;
        }
    }
}
```

**职责**：
- 维护背包格子数组
- 负责堆叠、拆分、整理
- 负责 `byte[]` 与 `BagItem[]` 之间的转换

**关键理解**：
- 它是布局管理器，不是资产真相源。

---

## 3. EquipManager

### 3.1 客户端 EquipManager

**文件**: `Src/Client/Assets/Scripts/Managers/EquipManager.cs`

```csharp
public class EquipManager : Singleton<EquipManager>
{
    public event OnEquipChangeHandler OnEquipChanged;
    public Item[] Equips = new Item[(int)EquipSlot.SlotMax];

    public void OnEquipItem(Item equip)
    {
        if (this.Equips[(int)equip.EquipInfo.slot] != null && this.Equips[(int)equip.EquipInfo.slot].Id == equip.Id)
        {
            return;
        }
        this.Equips[(int)equip.EquipInfo.slot] = ItemManager.Instance.Items[equip.Id];
        if (OnEquipChanged != null)
        {
            OnEquipChanged();
        }
    }
}
```

**职责**：
- 维护装备槽位数组
- 提供穿脱装备入口
- 在装备变化时通知 UI 刷新

**关键理解**：
- `Equips` 保存的是对 `ItemManager` 中 `Item` 对象的引用。

### 3.2 服务端 EquipManager

**文件**: `Src/Server/GameServer/GameServer/Managers/EquipManager.cs`

```csharp
public class EquipManager : Singleton<EquipManager>
{
    internal Result EquipItem(NetConnection<NetSession> sender, int slot, int itemId, bool isEquip)
    {
        Character character = sender.Session.Character;
        if (!character.ItemManager.Items.ContainsKey(itemId))
        {
            return Result.Failed;
        }
        UpdteEquip(character.Data.Equips, slot, itemId, isEquip);
        DBService.Instance.Save();
        return Result.Success;
    }
}
```

**职责**：
- 校验物品是否存在
- 更新角色装备栏数据
- 保存数据库

---

## 4. QuestManager

### 4.1 客户端 QuestManager

**文件**: `Src/Client/Assets/Scripts/Managers/QuestManager.cs`

**核心字段**：

| 字段 | 作用 |
|---|---|
| `questInfos` | 服务端下发的任务状态原始数据 |
| `allQuests` | 当前所有有效任务 |
| `npcQuests` | 按 NPC 维度组织的任务状态索引 |
| `onQuestStatusChanged` | 任务状态变化通知 |

**核心职责**：
- 初始化任务列表
- 推导可接任务
- 推导 NPC 当前状态
- 打开任务对话框
- 刷新任务状态

### 4.2 服务端 QuestManager

**文件**: `Src/Server/GameServer/GameServer/Managers/QuestManager.cs`

```csharp
class QuestManager
{
    Character Owner;

    public Result AcceptQuest(NetConnection<NetSession> sender, int questId)
    {
        Character character = sender.Session.Character;
        QuestDefine quest;
        if (DataManager.Instance.Quests.TryGetValue(questId, out quest))
        {
            var dbquest = DBService.Instance.Entities.CharacterQuests.Create();
            dbquest.QuestID = quest.ID;
            if (quest.Target1 == QuestTarget.None)
            {
                dbquest.Status = (int)QuestStatus.Complated;
            }
            else
            {
                dbquest.Status = (int)QuestStatus.InProgress;
            }
            sender.Session.Response.questAccept.Quest = this.GetQuestInfo(dbquest);
            character.Data.Quests.Add(dbquest);
            DBService.Instance.Save();
            return Result.Success;
        }
        else
        {
            sender.Session.Response.questAccept.Errormsg = "任务不存在";
            return Result.Failed;
        }
    }
}
```

**职责**：
- 接任务时创建任务记录
- 交任务时更新状态
- 发放金币、道具等奖励

---

## 5. ItemService / QuesService / QuestService

### 5.1 客户端 ItemService

**文件**: `Src/Client/Assets/Scripts/Services/ItemService.cs`

**职责**：
- 发送购买请求
- 发送装备请求
- 处理购买结果与装备结果回包

### 5.2 客户端 QuesService

**文件**: `Src/Client/Assets/Scripts/Services/QuesService.cs`

**职责**：
- 发送接任务请求
- 发送交任务请求
- 收到响应后驱动 `QuestManager` 刷新状态

### 5.3 服务端 QuestService

**文件**: `Src/Server/GameServer/GameServer/Services/QuestService.cs`

**职责**：
- 接收 `QuestAcceptRequest`
- 接收 `QuestSubmitRequest`
- 转发到角色 `QuestManager` 处理并回包

### 5.4 服务端 ItemService

**文件**: `Src/Server/GameServer/GameServer/Services/ItemService.cs`

**职责**：
- 接收购买请求并转给商店逻辑
- 接收装备请求并转给服务端 `EquipManager`

---

## 6. UI 核心类

### 6.1 UIBag

**文件**: `Src/Client/Assets/Scripts/UI/Bag/UIBag.cs`

**职责**：
- 读取 `BagManager.Items` 初始化背包格子
- 根据 `ItemManager` 取图标定义
- 响应整理背包按钮

### 6.2 UICharEquip

**文件**: `Src/Client/Assets/Scripts/UI/Eqyup/UICharEquip.cs`

**职责**：
- 展示可装备物品列表
- 展示已穿装备槽位
- 响应穿脱操作
- 刷新属性面板

### 6.3 UIQuestSystem

**文件**: `Src/Client/Assets/Scripts/UI/Quest/UIQuestSystem.cs`

**职责**：
- 展示主线/支线任务列表
- 根据当前 Tab 切换“已接任务 / 可接任务”视图
- 选中任务后刷新详情面板

---

## 7. 快速记忆版

如果只记最关键的类，可以记这几组：

| 类 | 一句话定位 |
|---|---|
| `ItemManager` | 玩家道具资产中心 |
| `BagManager` | 背包格子布局中心 |
| `EquipManager` | 装备槽位视图中心 |
| `QuestManager` | 任务集合与 NPC 状态中心 |
| `ItemService` | 购买与装备通信入口 |
| `QuesService` | 接任务与交任务通信入口 |
| `QuestService` | 服务端任务请求入口 |
| `UIBag` | 背包表现层 |
| `UICharEquip` | 装备与属性表现层 |
| `UIQuestSystem` | 任务列表表现层 |
